spring.application.name=eshop
google.api.key=AIzaSyDt_ujIOQH6IskE17wFEhZWl_gHtHZmGhQ

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=user
spring.datasource.password=user
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Port (optional, defaults to 8080)
server.port=8080

# Format SQL queries for better readability
spring.jpa.properties.hibernate.format_sql=true

# Log SQL queries with parameters (useful for debugging)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


# Set the base URL for Swagger UI
springdoc.swagger-ui.path=/swagger-ui.html

# Disable swagger-ui if needed
springdoc.swagger-ui.enabled=true

# OpenAPI description path
springdoc.api-docs.path=/v3/api-docs


